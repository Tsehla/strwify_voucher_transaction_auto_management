{
  "addons": [
    "mongolab"
  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "description": "This is used to run test  on heroku  for every commit. Not needed by application",
  "env": {
    "MONGODB_URI": {
      "required": true
    },
    "TZ": {
      "required": true
    },
    "GMAIL_EMAIL": {
      "required": true
    },
    "GMAIL_PASSWORD": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "name": "app.json for heroku",
  "scripts": {
  },
  "stack": "heroku-18"
}
